slam_toolbox:
  ros__parameters:
    use_sim_time: true
    scan_topic: /scan_fixed
    autostart: true

    # 🔥 CRITICAL FIX
    max_laser_range: 8.0

    # Good defaults (add these too)
    minimum_time_interval: 0.5
    transform_timeout: 0.2
    tf_buffer_duration: 30.0

    resolution: 0.05